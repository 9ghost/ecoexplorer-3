<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= activity?.id ? 'Editar' : 'Adicionar' %> Atividade</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1><%= activity?.id ? 'Editar' : 'Adicionar' %> Atividade</h1>
    <form action="<%= activity?.id ? '/activities/edit/' + activity.id : '/activities/new' %>" method="POST">
      <div class="mb-3">
        <label for="name" class="form-label">Nome</label>
        <input type="text" class="form-control" id="name" name="name" value="<%= activity?.name %>" required>
      </div>
      <input type="hidden" name="destinationId" value="<%= destinationId %>">
      <button type="submit" class="btn btn-primary">Salvar</button>
    </form>
  </div>
</body>
</html>
